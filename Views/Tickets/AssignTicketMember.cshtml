@model TicketMemberViewModel

@{
    ViewData["Title"] = "Assign Ticket Member";
}

<div class="d-flex justify-content-center mt-5 flex-column align-items-center">
    <h3 class="my-5">Assign Ticket Members</h3>
    <div class="card">
        <div class="card-header">
            <div class="card-title">@Model.Ticket.Title </div>
        </div>
        <div class="card-body">
            <form asp-action="AssignTicketMember" asp-controller="Tickets" method="post" class="d-flex flex-column">
                <input type="hidden" asp-for="Ticket!.Id" />
                <select asp-items="@Model.UserList" asp-for="DeveloperId" class="form-control" required>
                    <option value=""></option>
                </select>
                <span asp-validation-for="DeveloperId" class="text-danger"></span>
                <button type="submit" class="btn btn-primary my-3">Save</button>
                <a asp-action="Details" asp-route-id="@Model.Ticket!.Id" class="btn btn-secondary">Cancel</a>
            </form>
        </div>
    </div>
</div>